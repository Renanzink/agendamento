spring.application.name=Agendamento

# Credenciais do Gmail (Protegendo a senha)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=contato.emyllearq@gmail.com
# O Railway lerá a senha de uma variável segura que configuraremos lá
spring.mail.password=${MAIL_PASSWORD:mnno vsao dmqc pgbe}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Banco de Dados (Configuração para Railway)
# O Railway preenche essas variáveis ${MYSQL...} automaticamente para você
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:db_agendamentos}?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:admin123}

# Configurações do Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect